define('data/dashboard-data', function () {
    // placeholder for data/dashboard-data.js file loaded from couchdb
    // this MUST be excluded from the requirejs optimizer build
    return {projects: []};
});
