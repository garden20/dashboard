define('dashboard-data', function () {
    // placeholder for dashboard-data.js file loaded from couchdb
    // this MUST be excluded from the requirejs optimizer build
    return {databases: []};
});
